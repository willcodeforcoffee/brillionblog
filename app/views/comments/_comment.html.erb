<div class="py-4" id="<%= dom_id(comment) %>">
  <p class="font-medium"><%= comment.author_name %></p>
  <p class="text-xs text-gray-500"><%= comment.created_at.to_fs(:long) %></p>
  <p class="mt-1"><%= simple_format(comment.body) %></p>
</div>
